# Todo-web-app
This todo web app enables the user handle his/her tasks easily by scheduling it. since it stores the data in local storage of the browser it doesn't reset the stored tasks after the page refreshes. Also it pushes notification before the deadline of the tasks. 
